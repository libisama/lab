#include <iostream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    int a, b, a1, a2;
    cout << "Введите двузначное число." << endl;
    cin >> a;
    a1 = a / 10;
    a2 = a - a1 * 10;
    b = a2 * 10 + a1;
    cout << b << endl;
    return 0;
}
