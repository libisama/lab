#include <iostream>

using namespace std;

int main()
{
    setlocale(LC_ALL, "Russian");
    float a, b, x;
    cout << "Введите коэффициенты А и В (А не равен 0)." << endl;
    cin >> a >> b;
    x = -b / a;
    cout << "X = " << x << endl;
    return 0;
}
